
namespace CSharp_Library.Utility {

    /// <summary>
    /// Gives an appropiate name for the number to return from CompareTo()
    /// </summary>
    enum Comparison {
        Before = -1,
        Same = 0,
        After = 1
    }
}
